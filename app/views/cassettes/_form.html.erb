<%= form_with(model: cassette, local: true) do |form| %>
  <% if cassette.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cassette.errors.count, "error") %> prohibited this cassette from being saved:</h2>

      <ul>
      <% cassette.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :film_id %>
    <%= form.text_field :film_id %>
  </div>

  <div class="field">
    <%= form.label :format %>
    <%= form.text_field :format %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
